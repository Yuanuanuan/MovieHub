import MainWrapper from "@/layouts/MainWrapper";
import UpcomingSlide from "@/layouts/UpcomingSlide";

const Home = () => {
  return (
    <main>
      <MainWrapper />
      <UpcomingSlide />
    </main>
  );
};

export default Home;
